# Stale unpack dirs from manual IPK inspection
build/ipk/etc/
build/ipk/usr/
build/ipk/data/

# SDK / local build artifacts
build/sdk/
build/*.tar.zst
dist/
*.apk

# Editor / OS
*~
.DS_Store
Thumbs.db

# Local infra tests (real IPv6 prefixes, dev hosts) — never commit
tests/*.local.sh
tests/test_detect_lan_prefix.local.sh
